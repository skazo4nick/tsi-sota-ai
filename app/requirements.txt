# This project now uses the root requirements.txt for pip-based dependencies
# and app/environment.yml for Conda environments.
#
# Please refer to the main README.md for instructions on setting up your environment.
#
# To install dependencies using pip, use the root requirements file:
# pip install -r ../requirements.txt
#
# To set up a Conda environment for the app and notebooks:
# conda env create -f environment.yml
# conda activate article_downloader_env
